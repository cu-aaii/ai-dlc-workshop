"""The `dashboard` blueprint's Python packages.

Namespace anchor only -- deliberately no re-exports. U-02's code imports from
`dashboard.core`, whose `__init__` declares the one public surface (see NFR Design Q4).
"""
